# Deprecated: superseded by deploy-pages.yml.
# Kept as a manual-only stub so it cannot race with deploy-pages.yml.
# Safe to delete this file from your repo whenever convenient.
name: Deploy to GitHub Pages (deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is deprecated. Use deploy-pages.yml instead."
